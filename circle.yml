dependencies:
  pre:
    - sudo LC_ALL=en_US.UTF-8 add-apt-repository ppa:ondrej/php; sudo apt-get update; sudo apt-get install php7.0-cli
  override:
    - mkdir -p build/logs
    - composer install --dev --no-interaction
    - sed -i 's/^;//' ~/.phpenv/versions/$(phpenv global)/etc/conf.d/xdebug.ini

test:
  override:
    - php vendor/bin/phpunit
    - php vendor/bin/coveralls -v


general:
  artifacts:
    - "build"